import React from 'react';
import './index.scss';
declare type Props = {
    className?: string;
    style?: React.CSSProperties;
};
export default function TopNav({ className, style, }: Props): JSX.Element;
export {};
