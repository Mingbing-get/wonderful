export type BadgeProps = {
  children?: React.ReactNode,
  color?: string,
  count: number | React.ReactNode,
  dot?: boolean,
  offsetRight?: number,
  offsetTop?: number,
  overflowCount?: number,
  showZero?: boolean,
}