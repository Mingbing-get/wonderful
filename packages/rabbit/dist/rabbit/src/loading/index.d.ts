/// <reference types="react" />
import './index.scss';
declare type Props = {
    size?: number;
    color?: string;
};
export default function Loading({ size, color }: Props): JSX.Element;
export {};
