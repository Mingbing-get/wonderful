import React from 'react';
export declare type Props = {
    title: React.ReactNode;
    key: React.Key;
    children: React.ReactElement;
};
export default function TabItem({}: Props): JSX.Element;
