import React from 'react';
import './index.scss';
declare type Props = {
    children: React.ReactNode;
    className?: string;
    style?: React.CSSProperties;
};
export default function InputGroup({ children, className, style }: Props): JSX.Element;
export {};
